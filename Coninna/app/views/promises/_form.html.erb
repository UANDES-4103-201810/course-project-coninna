<%= form_for(promise) do |f| %>
  <% if promise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(promise.errors.count, "error") %> prohibited this promise from being saved:</h2>

      <ul>
      <% promise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :delivery_date %>
    <%= f.date_select :delivery_date, :start_year => 1920, :end_year => 2050%>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_area :description %>
  </div>

  <div class="field">
    <%= f.label :amount %>
    <%= f.number_field :amount %>
  </div>
  <%= f.hidden_field :project_id, :value => @project %>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
