<%= form_for(show_my_wishlist) do |f| %>
  <% if show_my_wishlist.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(show_my_wishlist.errors.count, "error") %> prohibited this wishlist from being saved:</h2>

      <ul>
      <% show_my_wishlist.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %>
    <%= f.text_field :user_id %>
  </div>

  <div class="field">
    <%= f.label :project_id %>
    <%= f.text_field :project_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
